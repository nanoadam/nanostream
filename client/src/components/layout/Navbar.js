import React from "react";
import PropTypes from "prop-types";

const Navbar = (props) => {
  return (
    <div>
      <h1>Navbar</h1>
    </div>
  );
};

Navbar.propTypes = {};

export default Navbar;
